一、去一家公司面试的编程题：
字符串匹配问题：字符“*”可代表任意多个的任意字符。
输入：一个不带“*”的字符串和一个通配字符串
输出：如果匹配，则输出“Matched”；否则，输出“Not Matched”

二、设计一个整数集合类，具有置空、添加元素、判断元素是否在集合中、求交集、输出显示集合元素等功能。实现方法(定义一个集合类set,   其数据成员有：集合大小setsize和存储数组list[100]。其成员函数有：置空操作empty()，添加元素操作add()，判断元素是否在集合中的操作ismember()、判断集合是否为空的操作isempty()、求交集操作intersetction()和输出显示操作print())。 

三   自定义矩阵的运算(编写一个程序，实现矩阵的加法、减法和乘法运算。首先定义一个矩阵类，用rows和cols存放行和列的序号，并用new   表达式动态创建double型数组，用于存储矩阵中各元素的值；然后重载加法、减法和乘法运算符，实现相应的矩阵运算；重载输出运算符“ <
<”，以行列顺序显示矩阵加的值，重载调用运算符“()”，并将其返回类型设为引用，完成矩阵中元素的读写。输入输出运算符的重载通常采用友元函数方式，格式如下：friend   istream   &operator> > (istream&in, <参数> )；friend   ostream   &operator < <(istream&in, <参数> ),其中，输入运算的重载用于完成从   istream类的流对象in输入参数中指定的数据，输出运算符的重载用于完成从ostream类的流对象   out输出参数中指定的数据。) 
